# mcp-wallet 环境变量示例（占位）。复制为 .env 后填入真实值；.env 已被 git 忽略。
# 基座阶段尚无运行时配置；各 key 由对应子任务实现 config 时落地（koanf：默认->文件->env->flag）。

# coord-server 监听地址（MPC-008）
COORD_LISTEN_ADDR=127.0.0.1:9100

# relay-node 监听地址（MPC-006）
RELAY_LISTEN_ADDR=127.0.0.1:9200

# 日志级别：debug|info|warn|error
LOG_LEVEL=info
